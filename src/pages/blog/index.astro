---
import { getCollection } from 'astro:content';

import PostCard from '../../components/PostCard.astro';
import BaseLayout from '../../layouts/BaseLayout.astro';
import { categories } from '../../data/categories';
import { getFeaturedPosts, getPublishedPosts } from '../../utils/content';

const allPosts = getPublishedPosts(await getCollection('blog'));
const featuredPosts = getFeaturedPosts(allPosts).slice(0, 3);
---

<BaseLayout
  title="Editorial Blog"
  description="Browse Urban Harvest Lab articles on urban gardening, hydroponics, grow lights, soil, containers, and plant troubleshooting."
  canonicalPath="/blog/"
>
  <section class="page-section">
    <div class="container page-hero">
      <div class="page-hero__panel">
        <span class="eyebrow">Editorial blog</span>
        <h1>Long-form growing guides with a clear point of view.</h1>
        <p>
          The blog is organized for depth, readability, and future expansion. Start with cornerstone guides or browse
          by category depending on your current growing constraint.
        </p>
      </div>
    </div>

    <div class="container">
      <div class="section-heading section-heading--compact">
        <span class="eyebrow">Featured reads</span>
        <h2>Start with the pieces that define the site.</h2>
      </div>

      <div class="card-grid card-grid--three">
        {featuredPosts.map((post) => <PostCard post={post} />)}
      </div>
    </div>
  </section>

  <section class="page-section">
    <div class="container">
      <div class="section-heading">
        <span class="eyebrow">All articles</span>
        <h2>Current publishing library</h2>
        <p>
          Browse every published article or jump into a category:
          {
            categories.map((category, index) => (
              <>
                {' '}
                <a href={`/${category.slug}/`}>{category.shortTitle}</a>
                {index < categories.length - 1 ? ',' : '.'}
              </>
            ))
          }
        </p>
      </div>

      <div class="card-grid">
        {allPosts.map((post) => <PostCard post={post} />)}
      </div>
    </div>
  </section>
</BaseLayout>
