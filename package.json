{
  "name": "urbanharvestlab",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "clean:astro-cache": "node scripts/clear-astro-cache.mjs",
    "predev": "npm run clean:astro-cache",
    "dev": "astro dev",
    "prestart": "npm run clean:astro-cache",
    "start": "astro dev",
    "prebuild": "npm run clean:astro-cache",
    "build": "astro build",
    "preview": "astro preview",
    "precheck": "npm run clean:astro-cache",
    "check": "astro check"
  },
  "dependencies": {
    "astro": "^5.0.0"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.6",
    "@types/node": "^22.10.2",
    "typescript": "^5.7.2"
  }
}
